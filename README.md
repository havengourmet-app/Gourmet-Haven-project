# QuickDyne Workspace

The active application now lives inside `QuickDyne/`.

## Where to work

- Main project docs: `QuickDyne/README.md`
- Progress tracker: `QuickDyne/progress.md`
- Frontend app: `QuickDyne/frontend`
- Backend app: `QuickDyne/backend`
- Supabase schema: `QuickDyne/supabase/migrations/0001_initial_schema.sql`
- Archived static prototype: `legacy-static-prototype/`

## Repository note

The older static HTML, CSS, and JS prototype has been archived in `legacy-static-prototype/` after the React migration. New implementation work should continue inside `QuickDyne/`.
